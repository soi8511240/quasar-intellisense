{
  "compilerOptions": {
    "target": "es6",                           // 컴파일된 JavaScript 버전
    "module": "commonjs",                      // 모듈 시스템
    "outDir": "./out",                         // 컴파일된 파일 위치
    "strict": true,                            // 엄격한 타입 체크
    "esModuleInterop": true,                   // ES 모듈과 함께 CommonJS 모듈 사용 가능
    "skipLibCheck": true,                      // 라이브러리 검사 생략
    "forceConsistentCasingInFileNames": true   // 파일 이름의 일관된 케이스 강제
  },
  "include": [
    "src/**/*.ts"                             // 컴파일할 TypeScript 파일 위치
  ],
  "exclude": [
    "node_modules"                            // 제외할 디렉터리
  ]
}
